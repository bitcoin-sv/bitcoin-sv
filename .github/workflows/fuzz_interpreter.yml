# Copyright (c) 2024 BSV Blockchain Association
# Distributed under the Open BSV software license,
# see the accompanying file LICENSE.

name: Fuzz - interpreter

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  fuzz_interpreter:
    runs-on: [ self-hosted, docker, large ]
    container:
      image: nchain/svn_dev:ubuntu_22.04
    steps:
      - name: No-op
        run: echo "Placeholder for interpreter fuzzing"


